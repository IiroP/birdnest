{
  "name": "birdnest-reaktor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "shx rm -rf build && cd ../birdnest-frontend/ && npm run build && shx cp -r build ../backend"
  },
  "author": "Iiro Poutiainen",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "xml2js": "^0.4.23"
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "shx": "^0.3.4"
  }
}
